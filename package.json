{
  "name": "hikari-tractors-storefront-reference-v5",
  "private": true,
  "version": "5.0.0",
  "description": "Hikari Tractors reference-locked marketplace storefront with exploded-diagram RFQ ordering.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node scripts/verify.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit": "npm run check && npm test && node scripts/audit-v4.mjs"
  }
}

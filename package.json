{
  "name": "hikari-tractors-storefront-pixel-exact-v7",
  "private": true,
  "version": "7.0.0",
  "description": "Hikari Tractors storefront with the approved pixel-exact desktop and mobile marketplace homepage, preserving catalog, diagram and RFQ behavior.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "lint": "node scripts/lint.mjs",
    "type-check": "node scripts/type-check.mjs",
    "build": "npm run lint && npm run type-check && npm run check && npm test",
    "check": "node scripts/verify.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit": "npm run check && npm test && node scripts/audit-v4.mjs",
    "qa:responsive": "node scripts/home-responsive-qa.mjs"
  }
}
